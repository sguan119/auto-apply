# CLAUDE.md

本文件为 Claude Code 提供项目上下文。

## 项目简介

**全自动投递脚本** —— 一个全自动求职投递工具（开源项目）。

整体流程：**搜索职位 → 改写简历 → 自动投递**。

## 三大模块

模块之间**高度独立**，彼此通过明确定义的数据契约（输入/输出）交互，互不依赖内部实现。

1. **搜索（search）** —— 按条件搜索职位，输出职位列表。
2. **改简历（resume）** —— 根据目标职位改写/适配简历，输出定制简历。
3. **投递（deliver）** —— 把简历投递到对应职位/平台，输出投递结果。

> 设计原则：每个模块可以独立开发、独立测试、独立替换。一个模块只认数据契约，不关心另一个模块怎么实现。

## 路线图

1. **CLI** —— 先实现命令行版本（当前阶段）。
2. **Website** —— 在核心逻辑之上加 Web 界面。
3. **Docker** —— 打包成 Docker 镜像发布。

> 架构尚未最终确定，见下方 "待定" 一节。

## 关键约束

- **开源项目**：代码清晰、文档完整、易于他人贡献。注意不要把密钥、cookie、个人简历等敏感数据提交进仓库。
- **核心逻辑与界面分离**：CLI 和未来的 Website 应共用同一套核心逻辑，界面只是薄薄的一层入口。
- **模块解耦**：跨模块通信只走数据契约，不要在模块间产生隐式耦合。

## 待定（架构决策）

- 语言 / 技术栈
- 模块间的数据契约格式（如 JSON schema）与传递方式（内存调用 / 文件 / 队列）
- 配置与密钥管理方式
- 各招聘平台适配层的抽象方式

## 开发约定

> 随项目推进补充：构建命令、测试命令、目录结构、代码风格等。
